// import type { Theme } from '@material-ui/core'
import { makeStyles } from '@material-ui/styles'

export default makeStyles(() => ({
  conatiner: { width: '100%', backgroundColor: '#fff' },
  toolbar: {
    display: 'flex',
    justifyContent: 'flex-end',
  },
}))
